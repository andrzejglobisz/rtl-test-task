version: '3.4'
services:
  scrapper-svc:
    image: ./some-prod-image
    env_file: ./services/scrapper-svc/config.prod.env
    restart: always
    expose:
      - "4010"

  movies-mongo:
    restart: always
    volumes:
      - ./mongo/movies/data/db:/data/db
    expose:
      - "27017"
